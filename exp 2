AIM:
The goal is to create a program that reads a C source code file and identifies individual tokens such as identifiers,
keywords, constants, operators, preprocessor directives, header files and delimiters, using FLEX and its built-in
regular expression matching.
ALGORITHM:
Start by defining patterns (using regular expressions) for each type of token (keywords, identifiers, numbers,
operators, delimiters, etc.).
Set up a FLEX (.l) file with three parts:
o Definitions
o Rules
o C code (main function)
In the rules section, match each pattern with an action (e.g., print &quot;Keyword&quot; if a keyword is found).
Compile the FLEX program using the flex and gcc commands.
Run the compiled program and give it some C code as input.
The program will scan the input and print out each token type.
Stop after all tokens are processed.
PROCEDURE:
Create a file with .l extension, lexer.l, where you write the FLEX code.
Inside the file, define how to recognize tokens using regular expressions.
Use the flex command to convert your .l file into a C program (lex.yy.c).
Compile the generated C program using gcc to produce an executable.
Run the executable and give it input C code.
The output will show you which tokens were recognized (Keyword, Identifier, Number, Operator, Delimiter, etc.).
PROGRAM:
lexer.l
%{
#include &lt;stdio.h&gt;
%}
KEYWORD int|float|char|double|void|for|while|if|else|return|struct|switch|case|break|do
%%
&quot;#include&quot; { printf(&quot;Preprocessor Directive : %s\n&quot;, yytext); }
&quot;&lt;&quot;[a-zA-Z.]+&quot;&gt;&quot; { printf(&quot;Header File : %s\n&quot;, yytext); }
{KEYWORD} { printf(&quot;Keyword : %s\n&quot;, yytext); }
[a-zA-Z_][a-zA-Z0-9_]* { printf(&quot;Identifier : %s\n&quot;, yytext); }
[0-9]+ { printf(&quot;Number : %s\n&quot;, yytext); }
&quot;==&quot;|&quot;&lt;=&quot;|&quot;&gt;=&quot; { printf(&quot;Operator : %s\n&quot;, yytext); }
&quot;+&quot;|&quot;-&quot;|&quot;*&quot;|&quot;/&quot;|&quot;=&quot;|&quot;&lt;&quot;|&quot;&gt;&quot; { printf(&quot;Operator : %s\n&quot;, yytext); }
[(){};,] { printf(&quot;Delimiter : %s\n&quot;, yytext); }
[ \t\n] { /* skip whitespace */ }
. { /* ignore rest */ }
%%
int yywrap() { return 1; }
int main(int argc, char *argv[]) {
if (argc &lt; 2) {
printf(&quot;Usage: %s &lt;input file&gt;\n&quot;, argv[0]);
return 1;
}
yyin = fopen(argv[1], &quot;r&quot;);
yylex();
printf(&quot;\nEnd of file\n&quot;);
return 0;
}

OUTPUT:
$ flex lexer.l
$ gcc lex.yy.c -o lexer -lfl
$ cat iplex.c
#include&lt;stdio.h&gt;
void main()
{
int x;
x = 10;
}
$ ./lexer iplex.c
Preprocessor Directive : #include
Header File : &lt;stdio.h&gt;
Keyword : void
Identifier : main
Delimiter : (
Delimiter : )
Delimiter : {
Keyword : int
Identifier : x
Delimiter : ;
Identifier : x
Operator : =
Number : 10
Delimiter : ;
Delimiter : }
End of file
RESULT:
Thus, the FLEX program for implementation of a Lexical Analyzer was executed and verified successfully.
